class Season {
  String id, number, name, overview, airdate, episodecount, posterurl, tvid;
  Season({
    this.id,
    this.number,
    this.name,
    this.overview,
    this.airdate,
    this.episodecount,
    this.posterurl,
  });
}
